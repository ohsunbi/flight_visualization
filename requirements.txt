# Fallback for environments that do not support uv.lock.
# pyproject.toml and uv.lock are the canonical dependency definitions.
streamlit==1.58.0
pandas==2.3.3
pyarrow==23.0.1
numpy==2.4.2
matplotlib==3.10.8
